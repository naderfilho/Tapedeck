name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: '24'

jobs:
  static:
    name: Lint, types and build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          # setup-node@v5 caches for the detected package manager by default. pnpm does not exist
          # yet at this point — the next step is what installs it — so the cache is asked for in
          # the second setup-node, below, and refused here.
          package-manager-cache: false

      - name: Enable pnpm
        run: corepack enable pnpm

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm format:check
      - run: pnpm lint
      - run: pnpm typecheck
      - run: pnpm build

  test:
    name: Tests on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # Both operating systems run the same determinism tests. A result that differs between
        # them would mean the reproducibility guarantee in ADR-0006 is not real.
        os: [ubuntu-latest, windows-latest]
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          # setup-node@v5 caches for the detected package manager by default. pnpm does not exist
          # yet at this point — the next step is what installs it — so the cache is asked for in
          # the second setup-node, below, and refused here.
          package-manager-cache: false

      - name: Enable pnpm
        run: corepack enable pnpm

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm coverage

      # The README states the test count and the coverage percentage in five places. They are
      # written from the run above rather than typed, and this fails the build if a commit left
      # them behind.
      - name: Documented figures match the run
        if: matrix.os == 'ubuntu-latest'
        run: pnpm counts:check

      - name: Upload coverage
        if: matrix.os == 'ubuntu-latest'
        uses: actions/upload-artifact@v5
        with:
          name: coverage
          path: coverage/
          retention-days: 7

  bench:
    name: Benchmark
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          # setup-node@v5 caches for the detected package manager by default. pnpm does not exist
          # yet at this point — the next step is what installs it — so the cache is asked for in
          # the second setup-node, below, and refused here.
          package-manager-cache: false

      - name: Enable pnpm
        run: corepack enable pnpm

      - uses: actions/setup-node@v5
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm build

      # Reported, never gated. A shared CI runner is the wrong place to assert on throughput; the
      # job exists so a regression is visible in the log next to the change that caused it.
      - name: Replay benchmark
        run: pnpm bench | tee -a "$GITHUB_STEP_SUMMARY"
